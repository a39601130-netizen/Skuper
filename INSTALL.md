# 🚀 ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ

## ШАГ 1: Подготовка окружения

### 1.1 Установка Python (если нет)
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip python3-venv

# Проверка
python3 --version  # Должно быть 3.10+
```

### 1.2 Создание папки проекта
```bash
mkdir ~/budget_bot
cd ~/budget_bot
```

### 1.3 Создание виртуального окружения
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
.\venv\Scripts\activate   # Windows
```

### 1.4 Установка зависимостей
```bash
pip install -r requirements.txt
```

---

## ШАГ 2: Настройка конфигурации

### 2.1 Создание .env файла
```bash
cp .env.example .env
nano .env  # или любой текстовый редактор
```

### 2.2 Заполнение .env файла

**TELEGRAM_BOT_TOKEN:**
1. Получен от @BotFather при создании бота
2. Формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

**GOOGLE_SHEETS_ID:**
1. Открой свою Google Sheets таблицу
2. Посмотри на URL: `https://docs.google.com/spreadsheets/d/ЭТОТ_ID_ЗДЕСЬ/edit`
3. Скопируй ID (длинная строка между `/d/` и `/edit`)

**DEEPSEEK_API_KEY:**
1. Зарегистрируйся на https://platform.deepseek.com/
2. Создай API Key в разделе "API Keys"
3. Формат: `sk-xxxxxxxxxxxxxxxxxxxx`

### 2.3 Размещение Google Credentials
1. Скопируй файл `google_credentials.json` в папку проекта
2. Убедись, что Service Account email добавлен в доступ к таблице

---

## ШАГ 3: Первый запуск

### 3.1 Тестовый запуск
```bash
# Активируй venv если еще не активировано
source venv/bin/activate

# Запуск
python main.py
```

### 3.2 Проверка работы
1. Открой Telegram
2. Найди своего бота по username
3. Отправь команду `/start`
4. Должно появиться приветствие и меню

### 3.3 Тест записи
1. Отправь: `50 продукты тест`
2. Проверь, что появилось в Google Sheets

---

## ШАГ 4: Запуск в фоне (production)

### Вариант A: Screen (простой)
```bash
# Установка screen
sudo apt install screen

# Создание сессии
screen -S budget_bot

# Запуск бота
source venv/bin/activate
python main.py

# Отсоединение от сессии: Ctrl+A, затем D
# Возврат к сессии:
screen -r budget_bot
```

### Вариант B: Systemd (надежный)
```bash
# Создание service файла
sudo nano /etc/systemd/system/budget_bot.service
```

Содержимое файла:
```ini
[Unit]
Description=Budget Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/home/ваш_пользователь/budget_bot
ExecStart=/home/ваш_пользователь/budget_bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация:
```bash
sudo systemctl daemon-reload
sudo systemctl enable budget_bot
sudo systemctl start budget_bot

# Проверка статуса
sudo systemctl status budget_bot

# Просмотр логов
journalctl -u budget_bot -f
```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "No module named 'telegram'"
```bash
pip install python-telegram-bot
```

### Ошибка: "gspread.exceptions.SpreadsheetNotFound"
1. Проверь GOOGLE_SHEETS_ID в .env
2. Убедись, что Service Account email добавлен в доступ к таблице

### Ошибка: "Invalid token"
1. Проверь TELEGRAM_BOT_TOKEN в .env
2. Убедись, что токен правильно скопирован

### Ошибка с DeepSeek API
1. Проверь баланс на platform.deepseek.com
2. Убедись, что API Key активен

### Бот не отвечает
1. Проверь, что бот запущен: `systemctl status budget_bot`
2. Проверь логи: `journalctl -u budget_bot -f`

---

## 📱 ИСПОЛЬЗОВАНИЕ БОТА

### Быстрый ввод (просто текст):
- `50 продукты пятерочка` → Расход
- `135 чаевые смена 10ч` → Доход с часами
- `перевод 100 карта` → Перевод

### Команды:
- `/start` - Главное меню
- `/add` - Добавить транзакцию
- `/balance` - Балансы счетов
- `/stats` - Статистика
- `/advisor` - AI советник
- `/history` - История
- `/help` - Справка

### Кнопки меню:
Используй inline кнопки для навигации

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ (по желанию)

1. **Автоматические уведомления**
   - Утреннее напоминание о записи
   - Предупреждения о превышении бюджета

2. **Расширенная статистика**
   - Графики расходов
   - Сравнение с прошлым месяцем

3. **Мультипользовательский режим**
   - Поддержка нескольких пользователей
   - Отдельные таблицы

4. **Монетизация**
   - Бесплатный план с ограничениями
   - Премиум с AI советником

---

*Создано с 💚 для Артура*
*Human Design: Эмоциональный Проектор 2/4*
