"""
Обработчики команд /start и /help
"""
from telegram import Update
from telegram.ext import ContextTypes
from bot.keyboards.menus import get_main_menu, get_reply_keyboard
import config

async def start_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /start"""
    
    user = update.effective_user
    
    welcome_text = f"""
👋 Привет, {config.USER_NAME}!

Я твой персональный **Budget Bot** 💰

Помогу тебе:
• 📝 Записывать расходы и доходы
• 💳 Отслеживать балансы счетов
• 📊 Анализировать статистику
• 🤖 Получать AI-советы по бюджету

**Быстрый ввод:**
Просто напиши в чат, например:
`50 продукты магазин`
`135 чаевые смена 10ч`

Или используй меню ниже 👇
"""
    
    await update.message.reply_text(
        welcome_text,
        parse_mode="Markdown",
        reply_markup=get_main_menu()
    )
    
    # Устанавливаем постоянную клавиатуру
    await update.message.reply_text(
        "⌨️ Клавиатура активирована",
        reply_markup=get_reply_keyboard()
    )


async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /help"""
    
    help_text = """
📚 *СПРАВКА ПО БОТУ*


*Команды:*
/start - Главное меню
/add - Добавить транзакцию
/balance - Показать балансы
/stats - Статистика за месяц
/advisor - AI советник
/history - История транзакций
/help - Эта справка


*Быстрый ввод:*
Формат: `сумма категория комментарий`

Примеры:
• `50 продукты пятерочка` - расход
• `135 чаевые смена 10ч` - доход с часами
• `перевод 100 карта` - перевод


*Сокращения категорий:*
• продукты, еда, магазин - Продукты
• чаевые, зп, зарплата - Зарплата/Чаевые
• такси, транспорт - соотв. категории


*Human Design советы:*
Бот учитывает твой профиль Эмоционального 
Проектора 2/4 при даче рекомендаций 🔮

Нужна помощь? Напиши /advisor
"""
    
    await update.message.reply_text(
        help_text,
        parse_mode="Markdown",
        reply_markup=get_main_menu()
    )
